здесь хранятся все фотографии, скорее всего, в основном, из новостей.

Имена указываются как:

имя-блока__имя-картинки.расширение

Например:

news-article__photo1.jpg

Если картинка используется в нескольких блоках, перечисляем их через _

news-article_news-column__photo1.jpg